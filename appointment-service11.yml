server:
  port: 8082

spring:
  application:
    name: APPOINTMENT-SERVICE11

  datasource:
    url: jdbc:mysql://localhost:3306/appointment_db
    username: root
    password: Adi@2552

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always
